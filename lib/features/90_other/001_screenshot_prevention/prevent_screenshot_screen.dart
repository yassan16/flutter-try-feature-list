import 'package:flutter/material.dart';

class PreventScreenshotScreen90001 extends StatefulWidget {
  const PreventScreenshotScreen90001({super.key});

  @override
  PreventScreenshotScreen90001State createState() =>
      PreventScreenshotScreen90001State();
}

class PreventScreenshotScreen90001State
    extends State<PreventScreenshotScreen90001> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Prevent Screenshot')),
      body: Center(child: Text('Prevent Screenshot Screen')),
    );
  }
}
